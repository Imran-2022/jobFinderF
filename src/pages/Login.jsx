import React from 'react';
import Layout from '../Layout';

const Login = () => {
    return (
        <Layout title="login page">
            <p>Login page !</p>
        </Layout>
    );
};

export default Login;