import React from 'react';
import Layout from '../Layout';

const AddJob = () => {
    return (
        <Layout title="add new Job">
            <p>Add new Jobs !</p>
        </Layout>
    );
};

export default AddJob;